import { Link, useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext';
export default function Layout({ children }) { const { user, logout } = useAuth(); const navigate = useNavigate(); const exit = () => { logout(); navigate('/login'); }; return <><header className="bg-slate-950 text-white"><nav className="mx-auto flex max-w-6xl items-center justify-between p-4"><Link className="text-xl font-bold text-cyan-300" to="/dashboard">Motorwise</Link><div className="flex items-center gap-4"><span className="hidden text-sm text-slate-300 sm:block">{user?.name}</span><button className="rounded bg-slate-700 px-3 py-1" onClick={exit}>Log out</button></div></nav></header><main className="mx-auto max-w-6xl p-4 sm:p-8">{children}</main></> }
