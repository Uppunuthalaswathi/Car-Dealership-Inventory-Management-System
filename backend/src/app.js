import express from 'express'; import cors from 'cors'; import authRoutes from './routes/authRoutes.js'; import carRoutes from './routes/carRoutes.js'; import { errorHandler, notFound } from './middleware/errorMiddleware.js'; import { ok } from './utils/response.js';
const app = express(); app.use(cors({ origin: process.env.CLIENT_URL || 'http://localhost:5173' })); app.use(express.json()); app.get('/api/health', (req, res) => ok(res, 200, 'API is running')); app.use('/api/auth', authRoutes); app.use('/api/cars', carRoutes); app.use(notFound); app.use(errorHandler); export default app;
